等有缘人做: 

1. 实现 rm 、move:
(1) rm 和 cp 类似, 但从下往上删. 
(2) move 只需改变父指针. 
(3) 都要处理移动了当前路径的情况. 
(4) 加入 rm 后, 所有用 fileIndex 判定文件是否存在都要改. 
(5) 加入一个废弃编号队列, 实现编号复用. 

2. g++ 指令编译不出正确的可执行程序. 

3. FileSystem::completePath() 函数的实现有误, 不在 root 下会出问题. 


